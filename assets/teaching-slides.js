(()=>{const deck=document.querySelector('.deck'),slides=[...document.querySelectorAll('.slide')];if(!deck||!slides.length)return;let current=0;const progress=document.createElement('div');progress.className='progress';document.body.append(progress);const hint=document.createElement('div');hint.className='hint';hint.textContent='← →  espacio  ·  Home / End';document.body.append(hint);slides.forEach((slide,index)=>{const foot=document.createElement('div');foot.className='footline';foot.innerHTML=`<span class="section">QVAC · ${document.title}</span><span class="num">${index+1} / ${slides.length}</span>`;slide.append(foot)});function fit(){const scale=Math.min(innerWidth/1920,innerHeight/1080);deck.style.transform=`scale(${scale})`;deck.style.left=`${(innerWidth-1920*scale)/2}px`;deck.style.top=`${(innerHeight-1080*scale)/2}px`}function show(next){current=Math.max(0,Math.min(slides.length-1,next));slides.forEach((s,i)=>s.classList.toggle('active',i===current));progress.style.width=`${(current+1)/slides.length*100}%`}addEventListener('resize',fit);addEventListener('keydown',e=>{if(['ArrowRight','ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();show(current+1)}if(['ArrowLeft','ArrowUp','PageUp'].includes(e.key)){e.preventDefault();show(current-1)}if(e.key==='Home')show(0);if(e.key==='End')show(slides.length-1)});let start=0;addEventListener('touchstart',e=>start=e.changedTouches[0].clientX,{passive:true});addEventListener('touchend',e=>{const d=e.changedTouches[0].clientX-start;if(Math.abs(d)>45)show(current+(d<0?1:-1))},{passive:true});fit();show(0)})();
