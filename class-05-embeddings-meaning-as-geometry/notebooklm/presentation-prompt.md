# NotebookLM — Presentation Prompt

Genera una presentación detallada sobre **Embeddings: Meaning as Geometry**.

Narrativa visual:

`keyword problem → vector representation → similarity → ranking → ambiguity → measurement → bridge to RAG`

Incluye slides de:

- embeddings vs generation;
- texto → vector;
- cosine similarity;
- QVAC `embed()` single vs batch;
- semantic search pipeline;
- Prediction antes del ranking;
- Measure It separando corpus/query/ranking;
- Break It con query ambigua;
- misconception: score ≠ truth;
- “Why this is not RAG yet”.

No inventes dimensiones, benchmarks ni APIs.