# NotebookLM — Audio Overview Prompt

Crea un **Análisis detallado** sobre **Clase 05 — Embeddings: Meaning as Geometry** usando solo las fuentes seleccionadas.

Idioma: español, manteniendo `embedding`, `vector`, `cosine similarity`, `Top-K` y nombres API en inglés.

Construye la conversación alrededor de:

1. por qué keyword search y semantic search no son lo mismo;
2. texto → embedding → vector;
3. por qué las dimensiones individuales no son etiquetas humanas simples;
4. embeddings vs generación;
5. cómo QVAC v0.18.x expone `loadModel()` + `embed()` + `unloadModel()`;
6. query embeddings vs document embeddings;
7. qué significa cosine similarity;
8. qué significa —y qué NO significa— un score alto;
9. por qué una query ambigua puede producir resultados razonables pero incorrectos para la intención;
10. qué se mide en el lab;
11. por qué esto todavía no es RAG;
12. cómo prepara la Clase 06.

Termina preguntando: **“Si el Top-1 te sorprende, ¿qué investigarías antes de decir que el modelo falló?”**